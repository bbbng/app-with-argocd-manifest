apiVersion: v1
kind: Service
metadata:
  name: argocd-service
spec:
  selector:
    app.kubernetes.io/name: argocd-web
  type: LoadBalancer
  ports:
  - protocol: TCP
    nodePort: 31001
    port: 80
    targetPort: 8080
